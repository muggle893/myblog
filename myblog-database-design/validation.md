# 数据库验证记录

日期：2026-09-12。

使用真实MySQL 8.0.46的初始化SQL执行通道，在临时数据目录执行交付的3份SQL；未启动网络监听，也未连接用户服务器。移除SQL注释并合并空白以适配init-file逐行执行，保留原SQL词元，不用其他数据库方言替代MySQL验证。

建库、建表、初始化数据和6组查询均执行成功。另通过19项数据库断言（14项正向断言与5项错误数据拦截）：

- 成功创建13张表：通过。
- 成功创建20个外键：通过。
- 默认账号禁用且无通用密码：通过。
- 游客列表只返回公开文章：通过。
- 作者只能看到公开文章和自己的私有文章：通过。
- 允许同一作者创建多份空白新草稿：通过。
- 版本匹配的草稿保存成功：通过。
- 过期的自动保存请求影响0行：通过。
- 自动保存不改变正式文章：通过。
- 移除草稿附件引用不影响正式文章附件：通过。
- 事务回滚恢复正式文章正文：通过。
- 发布后允许创建下一份活跃编辑草稿：通过。
- 发布后的草稿保留文章ID供重试返回：通过。
- 正式文章版本变化后旧稿条件更新影响0行：通过。
- 重复活跃修改草稿被唯一键拒绝：通过。
- 跨作者修改草稿被复合外键拒绝：通过。
- 封面位置超过100被CHECK拒绝：通过。
- 非法可见性被CHECK拒绝：通过。
- 仍被文章引用的文件禁止物理删除：通过。

图像验证：总图和3张分图均已渲染检查；13张表、99个字段、20个外键与DDL、字段字典及图源一致。

范围限制：验证的是DDL、约束、查询和事务语句，不是已实现的Java服务；未进行真实HTTP鉴权、文件下载权限、多线程并发压测，也未在MySQL8.4重复执行。发布结果字段已验证可保留重试结果，但幂等发布的业务分支仍需Service实现。
