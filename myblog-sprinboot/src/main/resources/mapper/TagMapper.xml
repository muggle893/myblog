<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.txf.myblogsprinboot.dao.TagMapper">
    <sql id = "tagFields">
        id, `name`
    </sql>
    <select id="getTagsByIds" resultType="org.txf.myblogsprinboot.vo.TagVO">
        SELECT <include refid="tagFields"></include>
        FROM blog_tag
        WHERE id IN
        <foreach collection="tagIds"
                 item="tagId"
                 open="("
                 separator=","
                 close=")">
            #{tagId}
        </foreach>
    </select>

    <select id = "getTagsByArticleIds" resultType="java.lang.Long">
        select tag_id
        from blog_article_tag
        where `article_id`
        in <foreach collection="articleIds" item = "articleId" open = "(" separator="," close=")">
                #{articleId}
            </foreach>
    </select>

    <insert id="insertTag" useGeneratedKeys="true" keyColumn="id" keyProperty="id">
        insert into blog_tag(name)
        values(#{name})
    </insert>
</mapper>